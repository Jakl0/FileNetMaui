<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="zad3filenetmaui.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label x:Name="countLabel"/>
            <CollectionView SelectionChanged="OnPlikSelected" SelectionMode="Single" x:Name="Lista">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout>
                            <Label Text="{Binding Nazwa}" Style="{StaticResource SubHeadline}"/>
                            <Label Text="{Binding Rozmiar , StringFormat='Rozmiar: {0} kB'}" Style="{StaticResource SubHeadline}"/>
                            <Button Text="Usun" Clicked="OnUsunClick" HorizontalOptions="Center" CommandParameter="{Binding}"/>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
